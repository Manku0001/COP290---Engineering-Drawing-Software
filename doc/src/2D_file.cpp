#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <fstream>
#include <math.h>
#include "graph.h"
using namespace std;

class _2D_file{
public:
	string get_2D_graph(vector<vector<co_ordinates_2D> > &cz,vector<string> &edges1,vector<string> &edges2,vector<string> &edges3){
		/*! This function prompts the user to first enter the name of the input file containing the 
		     vertices(along with co-ordinates) and edges of the 3 views of 3D-figure to be generated.
		* It stores the 2D-figures in a graph (outgraphs2) and the edges of the 3 views separately.*/
    }
}