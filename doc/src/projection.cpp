#include <iostream>
#include <string>
#include <vector>
#include <math.h>
#include <fstream>
#include <math.h>
#include "graph.h"
using namespace std;

class projection{
public:
	void projections(vector<co_ordinates_3D> &c,vector<vector<co_ordinates_2D> > &graphs){
		/*! In this function the graph containing the transformed 3D-view is converted into 
		    a 3 graphs containing the projections by using various projection matrices for 
		    each view and stored in a vector of 2D-graphs.*/
	}
}